/**
 * Edit these values once when you deploy.
 *
 * GitHub Pages (project site):
 *   url:  'https://YOUR-GITHUB-USERNAME.github.io'
 *   base: '/hsr-data-bank'          ← must match your repo name
 *
 * GitHub Pages (user site — repo named YOUR-GITHUB-USERNAME.github.io):
 *   url:  'https://YOUR-GITHUB-USERNAME.github.io'
 *   base: '/'
 *
 * Cloudflare Pages (custom domain or *.pages.dev):
 *   url:  'https://your-project.pages.dev'
 *   base: '/'
 */
export const site = {
  title: 'Data Bank',
  tagline: 'Trailblaze Campaign Archive',
  url: 'https://KATOONS-DATA.github.io',
  base: '/hsr-data-bank',
} as const;
